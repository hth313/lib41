.. index:: RTN?

.. object:: RTN?

Test whether there is a pending return address. In keyboard mode,
it will respond with either ``YES`` or ``NO``. In a program it will
skip the following line is there are no pending return address.
