.. index:: ATOXR

.. object:: ATOXR

Similar to ``ATOXR`` in the Extended Functions and HP-41CX, but
takes the character at the right end of alpha register instead and
returns its character code to X.
If the alpha register is empty, 0 is returned to X.
