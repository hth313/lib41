.. index:: DECODE

.. object:: DECODE

This is the counterpart of ``CODE``, it takes a hexadecimal number in
alpha register and converts it into binary and returns it
in X. Allowing you to create any value, real or non-normalized
numbers.
